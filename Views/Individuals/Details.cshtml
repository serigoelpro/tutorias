@model PlataformaWeb.Models.Individual

@{
    ViewBag.Title = "Details";
}

<h2>Imprimir</h2>

<style>
    iframe {
        width: 100%;
        border: 1px solid #969696;
    }
</style>

<div class="page-header">
    <h4>Seguimiento individual de @Model.Nombre     | F-PSE-DICA-07/R04</h4>
</div>


<hr />
@using ReportViewerForMvc;

@if (ViewBag.ReportViewer != null)
{
    <div class="alert alert-info" role="alert">
        Por favor espere a que cargue completamente el reporte.
        Para imprimir de clic en el icono de la impresora en el panel del reporte.
    </div>
    <div class="row">
        <div class="col-md-1"> </div>
        <div class="col-md-10">
            @Html.ReportViewer(ViewBag.ReportViewer as Microsoft.Reporting.WebForms.ReportViewer)
        </div>
        <div class="col-md-1"> </div>
    </div>
}

<a class="btn btn-primary" href="@Url.Action("Index",new { id = Model.IdPersona })"
   role="button">
    <span class="glyphicon glyphicon-arrow-left"></span>
    Regresar

</a>
@*<p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.IdIndividual }) |
        @Html.ActionLink("Back to List", "Index")
    </p>*@
